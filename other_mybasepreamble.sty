
%=================
% Mathematics
%=================
%\numberwithin{equation}{section} % reset equation count within sections etc.
%\numberwithin{figure}{section}
%\usepackage[draft]{showkeys}

%=================
% Encoding & fonts
%=================
%\usepackage{polyglossia}
%\setdefaultlanguage[variant=mono,numerals=arabic,attic=false]{greek}
%\newfontfamily\greekfont[Mapping=tex-text]{Tahoma}
%\setotherlanguage[variant=us]{english}

%\setmainfont[Mapping=tex-text]{Tahoma}
%\setmainfont[Mapping=tex-text,AutoFakeSlant=0.4]{Tahoma}
%\newfontfamily\em{Verdana Italic} % define alternative italic font, when fake slant is not used
%\newfontfamily\sc{Verdana SmallCaps}

%\usepackage[math-style=TeX]{unicode-math} % waiting for OpenType Math Support (Unicode) in Latin Modern fonts
%\setmathfont{Neo Euler}

% Neo Euler
% STIXGeneral
% XITS Math
% Cambria Math
% Asana Math % not working properly

%=================
% Import Graphics
%=================
%\usepackage[figuresright]{rotating}
%\usepackage{wrapfig}

%\usepackage[final]{pdfpages}
%\includepdf[pages=6-9]{insertme.pdf}
%\includepdf[pages=-]{insertme.pdf}
%\usepackage[space,extendedchars]{grffile} % allow spaces in picture path name

%=================
% Draw Graphics
%=================
%\usepackage{flow} % drawing flowcharts with pseudocode

%\usepackage{tikz,pgfplots,pgf,pgffor}
%\usepgflibrary{plothandlers} % {pgf,pgffor,pgfplots,plothandlers} needed for MATFIG2PGF
%\usetikzlibrary{shapes,arrows,matrix,decorations.pathreplacing,calc} % for flowchart creation

%=================
% Listings
%=================
\usepackage{textcomp} % symbols
\usepackage{listings} % source code formatting (CAUTION: listings != listing package)

\lstset{%
language=C,% choose the language of the code
basicstyle=\footnotesize,% the size of the fonts that are used for the code
numbers=left,% where to put the line-numbers
numberstyle=\footnotesize,% the size of the fonts that are used for the line-numbers
stepnumber=2,% the step between two line-numbers. If it's 1 each line will be numbered
numbersep=5pt,% how far the line-numbers are from the code
backgroundcolor=\color{white},% choose the background color. You must add \usepackage{color}
showspaces=false,% show spaces adding particular underscores
showstringspaces=false,% underline spaces within strings
showtabs=false,% show tabs within strings adding particular underscores
%frame=single,% adds a frame around the code
tabsize=4,% sets default tabsize to 2 spaces
captionpos=b,% sets the caption-position to bottom
breaklines=true,% sets automatic line breaking
breakatwhitespace=false,% sets if automatic breaks should only happen at whitespace
title=\lstname,% show the filename of files included with \lstinputlisting; also try caption instead of title
escapeinside={\%*}{*)},% if you want to add a comment within your code
frame=shadowbox,%
rulesepcolor=\color{blue},%
keywordstyle=\color[rgb]{0,0,1},%
commentstyle=\color[rgb]{0.133,0.545,0.133},%
stringstyle=\color[rgb]{0.627,0.126,0.941}%
}

%=================
% Tables
%=================
%\usepackage{multirow}
%\usepackage{tabularx}
%\usepackage{tabulary} % less apealling alternative is tabularx, example: \begin{tabulary}{0.75\textwidth}{|L|L|}
%\usepackage{booktabs} % included with ctable

%=================
% Auxiliary
%=================
%\usepackage[disable]{todonotes} %[disable]
% big "DRAFT" watermark
%\usepackage{draftwatermark}
%\SetWatermarkText{\textsf{DRAFT}}

%=================
% mypackages
%=================
\usepackage{myblockstyles}
